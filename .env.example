# The frontend now talks to the backend on the same origin (relative /api/v1
# paths), so request signatures can cover the exact path the server sees.
# In dev, vite.config.ts proxies /api to the backend on port 8202.
